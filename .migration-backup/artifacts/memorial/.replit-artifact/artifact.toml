kind = "web"
previewPath = "/"
title = "Página de Recuerdos"
version = "1.0.0"
id = "artifacts/memorial"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20534

[services.development]
run = "pnpm --filter @workspace/memorial run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/memorial", "run", "build" ]
publicDir = "artifacts/memorial/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20534"
BASE_PATH = "/"
